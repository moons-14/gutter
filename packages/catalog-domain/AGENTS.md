# Catalog domain

M2 owns deterministic rebuildable catalog identity only. Identity text is Unicode NFKC, trimmed,
Unicode-whitespace-collapsed and case-sensitive. Search keys are separate and must never be used
to merge entities.

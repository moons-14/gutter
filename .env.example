# Development only; set this to an untracked private value before starting Compose.
POSTGRES_PASSWORD=
# Base Compose requires POSTGRES_PASSWORD and derives DATABASE_URL from it.
# Outside Compose, set exactly one of DATABASE_URL or DATABASE_URL_FILE.
